{
  "dependencies": {
    "live-server": "^1.2.0",
    "puppeteer": "^24.17.0"
  },
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "build": "tsc",
    "serve": "live-server ./dist"
    },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "sirv-cli": "^3.0.1",
    "typescript": "^5.9.2",
    "dev": "tsc --watch"
  }
}
